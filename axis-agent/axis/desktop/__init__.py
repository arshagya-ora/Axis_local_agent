"""AXIS Phase 4: the PySide6/QML Windows desktop control surface.

A thin presentation and control layer over the accepted Phase 3 durable
client (`axis.durability.client`). Contains no browser, agent, policy,
workflow, or Temporal-internal logic of its own — see `backend.py`'s
module docstring for the narrow boundary this package is built around.
"""
